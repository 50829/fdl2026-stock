• 下一步我建议做两件事，优先级很明确。

  第一，补一组“序列长度消融”。现在 GRU/TCN 覆盖率只有
  80%，而且 seq_len=60 太苛刻。应该跑：

  GRU seq_len = 20 / 30 / 60
  TCN seq_len = 20 / 30 / 60

  统一用：

  EMA 最优 decay
  topk10_drop2
  valid 选模型
  test 只评估一次

  要看的不是单纯 IC，而是：

  覆盖率
  ICIR
  topk10_drop2 夏普
  最大回撤
  平均换手

  如果 seq_len=20/30 覆盖率明显提高，同时 ICIR 不掉太
  多，那深度模型融合才更有实际价值。

  第二，画最终报告图表。现在实验已经很多了，继续加模
  型的边际收益不一定高。报告最需要的是把已有结果讲清
  楚：

  1. LGB / XGB / LGB+XGB 主模型对比
  2. MLP / GRU / TCN raw vs EMA
  3. 树模型 + EMA 深度模型融合
  4. label1d vs label5d 区别
  5. 主模型月度 IC、年度 ICIR
  6. topk10_drop2 净值曲线
  7. 回撤曲线和换手曲线

  我的建议顺序：

  1. 先跑 seq_len=20/30 的 GRU/TCN 小实验
  2. 如果结果没有明显提升，就冻结模型实验
  3. 整理最终模型选择逻辑
  4. 画报告图
  5. 更新实验报告正文